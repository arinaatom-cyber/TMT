# PXD005410 — HeLa cervix (example protein table)

Demo folder for the atlas: `protein_table.csv` is loaded when you open **Cervix** and click **Show proteins** or **Load project proteins**.

The master spreadsheet column **Result Files** may list a different filename; the app also tries `protein_table.csv`, `proteins.csv`, and `proteins.tsv`.
